# YLYW V17→V18 对比分析与 V19 架构提案

## 一、V17与V18的本质差异：两条路线

### 1.1 V17：易理语义驱动路线（"大脑"路线）

```
task_desc → HanziEngine → 任务卦象
            ↓
      太极分解 → 子目标序列卦象
            ↓
  每步 obs → HanziEngine → 场景卦象
            ↓
  场景卦象 vs 任务卦象 → 差距卦象 → 意图涌现
            ↓
  意图 → 动作卦象 → ActionPrimitive → ALFWorld cmd
```

**核心哲学**：全程在卦象空间中推理。从任务理解到子目标分解到动作选择，全部由HanziEngine的六爻感知驱动。

**关键组件**：
- `HanziEngine.sentence()`：输入中文情景文本 → 输出8D八卦隶属度+6爻向量+64卦
- `_taiji_decompose()`：任务卦象 → "太极生两仪" → 子目标序列
- `_match_intent_by_gua()`：场景爻向量 → 欧氏距离匹配意图原型
- 差距卦象：`scene_yao - task_yao` 做进度诊断

**优点**：
- 易理是真正的"推理引擎"，不是装饰
- 子目标从卦象空间"涌现"而非if-else路由
- 情景卦象是完整语义理解，不是手工特征

**致命弱点**（~40%）：
- HanziEngine在英文环境下稳定性差
- obs文本解析噪声污染场景记忆
- **没有实例级世界模型**：记忆基于文本匹配，不可靠
- **没有执行护栏**：震荡/循环/空转无法无惩
- 意图→动作映射仍然是结构化处理（if-else导航），与卦象空间脱节

### 1.2 V18：符号规划主导路线（"小脑"路线）

```
task_desc → goal_parser(正则词表) → 结构化目标
            ↓
  world_model(实例级状态跟踪器) → 有穷状态
            ↓
  veto过滤(13条硬约束) + stuck恢复逻辑
            ↓
  YLYW评分器(仅作为最终选择器)
```

**成功之处**（79.9%冻结 → 98.5%转导）：
1. **实例级世界模型**：跟踪每个物体ID的位置、状态、是否访问过
2. **13条veto安全约束**：消灭震荡/循环/空转三大死因
3. **目标驱动的6维手工编码**：精确表达"做什么才算推进目标"
4. **stuck恢复逻辑**：frontier探索 + pending-target回访

**代价**：
- 易理从"推理引擎"降级为"评分器"，影响力仅4.4%
- 6维编码完全是手工公式（200+行if-else），不是"被感知的卦象"
- goal_parser是300行正则词表，零泛化能力
- 所有知识都是环境特定的（词表/先验/veto阈值）

### 1.3 关键洞见

| | V17 强 | V18 强 |
|---|---|---|
| 语义理解 | ✅ HanziEngine完整理解 | ❌ 正则词表 |
| 子目标规划 | ✅ 卦象空间涌现 | ❌ 符号阶段机 |
| 场景状态 | ❌ 文本解析噪声 | ✅ 实例级世界模型 |
| 执行护栏 | ❌ 几乎无 | ✅ 13条veto |
| 动作选择 | ❌ 意图→动作映射不强 | ✅ 目标驱动+重试链 |

## 二、V18的六爻编码：手工特征 vs 易理六爻

V18的6维编码本质上是**6个目标驱动的手工特征函数**：

```
y1（目标差距缩小）：if verb=take and is_target ⇒ 0.9 else if verb=put and dest ⇒ 0.95...
y2（持有承接）：if holding_target and verb=put ⇒ 0.9 else if empty_hand and take_target ⇒ 0.88...
y3（处理谓词推进）：if need_proc and verb=clean/heat/cool ⇒ 0.95...
y4（容器可供性）：if recep_state=open ⇒ 0.85 else if recep_state=closed and search ⇒ 0.75...
y5（目标关联）：if obj_cls matches goal ⇒ high else if recep_cls matches goal ⇒ moderate...
y6（新颖性-失败史）：if visited_and_failed ⇒ 0.05 else if never_visited ⇒ 0.85...
```

**它与真正的易理六爻有本质区别**：

| | V18 6维手工特征 | 易理六爻（yao_relations.py） |
|---|---|---|
| **语义** | 目标距离度量 | 整体情境的阴阳状态 |
| **维度含义** | 固定：差距/持有/处理/容器/关联/新颖 | 从下往上：基础→可达→力→脆弱→优先级→环境 |
| **关系分析** | 无——6个独立标量 | **乘承比应当位得中** |
| **输出** | 余弦匹配64卦模板 → favorability | 爻位关系报告 + 策略修正 |
| **状态来源** | 结构化world_model | HanziEngine感知的情景卦象 |

**V18的6维编码在工程上极其有效**——它的设计让每个维度精确对应"这个动作对任务目标的贡献是多少"。但没有"关系"——6个维度各自独立打分，没有乘承比应分析，没有当位得中。

**所以消融证明L3贡献≈0的真正原因**：不是卦象本身没用，而是**输入的6维编码已经是目标导向的线性度量，余弦匹配到64卦后加一个乘性favor因子，对排序的影响微乎其微**。如果6维编码是真正的"情境卦象"（包含爻位关系），L3的贡献会完全不同。

## 三、V19架构提案：大脑+小脑混合架构

### 3.1 核心理念

> **HanziEngine + YaoRelations（修正后的六爻）做"大脑"——负责理解情境、涌现意图、诊断困境。**
> **V18的world_model + veto + stuck恢复做"小脑"——负责执行落地、步骤导航、纠偏护栏。**
> **六爻编码从手工特征升级为HanziEngine感知的语义卦象 + YaoRelations的乘承比应分析。**

### 3.2 架构图

```
                    ┌─────────────────────────────────┐
                    │         V19 Agent                │
                    │                                  │
    ┌────────────┐  │  ┌─────────────────────────┐     │
    │ task_desc  │──┼─→│   易理推理层 (大脑)       │     │
    └────────────┘  │  │                         │     │
                    │  │ ① task_desc + 场景历史    │     │
                    │  │    → HanziEngine         │     │
                    │  │    → 8D八卦+6D情景卦象    │     │
                    │  │                         │     │
                    │  │ ② 情景卦象 → YaoRelations│     │
                    │  │    → 乘承比应当位得中     │     │
                    │  │    → 爻位关系报告         │     │
                    │  │                         │     │
                    │  │ ③ 差距卦象提取            │     │
                    │  │    scene_yao-task_yao    │     │
                    │  │    → 进度诊断+意图涌现    │     │
                    │  │                         │     │
                    │  └──────────┬──────────────┘     │
                    │             │                    │
                    │             ↓                    │
                    │  ┌─────────────────────────┐     │
                    │  │  符号规划层 (小脑)        │     │
                    │  │                         │     │
    ┌────────────┐  │  │ ④ world_model           │     │
    │ admissible │──┼─→│    实例级状态跟踪        │     │
    └────────────┘  │  │                         │     │
                    │  │ ⑤ 13条veto过滤           │     │
                    │  │    保留全部V18约束        │     │
                    │  │                         │     │
                    │  │ ⑥ 卦象辅助的动作排序      │     │
                    │  │    YaoRelations修正评分   │     │
                    │  │    + goal_gap线性评分     │     │
                    │  │                         │     │
                    │  │ ⑦ stuck恢复              │     │
                    │  │    增加易理诊断分支       │     │
                    │  └──────────┬──────────────┘     │
                    │             │                    │
                    └─────────────┼────────────────────┘
                                  ↓
                            ALFWorld action
```

### 3.3 模块级设计

#### 模块A：易理推理层（大脑）

**A1. 情境构造器**（新增）

将world_model的结构化状态 + goal + 历史摘要 → 情景文本。

V17已有此概念但只在debug模式启用。V19中这是核心输入。

```python
class ContextBuilder:
    """
    从world_model的结构化状态 + goal + 历史 → 情景文本。
    这个文本将被送入HanziEngine感知，产出卦象。
    """
    def build(self, world, goal, phase):
        # 位置：我在柜台前，看到盘子1放在柜台上
        # 持有：手里拿着盘子2
        # 历史：已洗过碗、已放好盘1
        # 目标：还要拿盘子2放到柜台
        # → 完整情景描述 → HanziEngine.sentence()
        ...
```

**A2. 情境卦象感知**（HanziEngine，已有）

```python
result = hanzi_engine.sentence(context_text)
# → {'main_hexagram': '䷕鼎', 'yao_vector': [0.72, 0.85, ...], 
#     'bagua': [0.12, 0.08, ...], 'score': 0.87}
```

**A3. 爻位关系分析**（YaoRelations，已有，需重映射）

```python
report = yao_relations.analyze(result['yao_vector'])
# → dangwei: 4/6, dezhong: ✓二✗五, cheng: 1处, ying: 1对
# → advice: "多爻当位，策略稳固；中位失据，需额外谨慎"
```

**A4. 差距卦象诊断**（核心创新）

```python
delta_yao = scene_yao - task_yao
# 哪个维度落后 → 当前策略偏差
# 当位分析 → 行动是否"得位"
# 乘承关系 → 外部阻力还是内部结构问题
```

差距卦象的6维与V18的手工6维形成对比：

| V18手工6维含义 | 差距卦象6维含义 |
|---|---|
| y1: 目标差距缩小（取值0.05-0.95） | y1: 行动-基础稳定性的偏差 |
| y2: 持有承接（0.05-0.95） | y2: 可达性-是否在正确位置 |
| y3: 处理谓词推进 | y3: 力需求-动作是否匹配需求 |
| y4: 容器可供性 | y4: 脆弱性-当前状态是否"险" |
| y5: 目标关联 | y5: 优先级-注意力是否得当 |
| y6: 新颖性-失败史 | y6: 环境约束-空间是否合适 |

**关键区别**：差距卦象的6维反映的是"整体情境的健康度"，而不是"这个候选动作的贡献度"。比如第五维（y5）是"当位性"——五爻代表"君位"，即当前行动是否符合整体策略——这是任何手工特征无法捕捉的抽象概念。

#### 模块B：符号规划层（小脑）

**B1. world_model**（直接从V18移植，不改）

全部保留V18的实例级状态管理：物体ID跟踪、位置、容器开合、访问状态、失败历史。

**B2. 13条veto安全约束**（直接从V18移植，不改）

全部保留V18的13条veto。易理层只能在存活候选中做排序，veto是安全红线。

**B3. 卦象辅助的动作评分**（新增混合评分）

```python
def score_alive_candidates(alive_cands, world, goal, yao_report):
    """
    混合评分：
    40% 差距卦象的"应否做此动作"（来自HanziEngine感知）
    40% 目标驱动线性分（V18的手工6维，作为"对目标贡献"）
    20% 爻位关系修正（来自YaoRelations的策略建议）
    """
    for cand in alive_cands:
        yao = build_yao(cand, world, goal)  # V18手工6维（保留）
        semantic_score = gap_yao_alignment(cand, delta_yao, yao_report)
        ylyw_score = goal_progress_score(yao)  # V18原始评分
        relation_mod = yao_report.strategy_modifier
        final = 0.4 * semantic_score + 0.4 * ylyw_score * relation_mod
        cand.update_score(final)
```

**B4. 易理诊断的stuck恢复**（新增分支）

V18的stuck恢复逻辑（frontier探索、pending-target回访）保留。额外增加一个易理诊断分支：

```python
# 当检测到stale ≥ 4步时
if stale >= 4 and yao_report:
    if yao_report.caution_level == 'very_cautious':
        # 卦象显示当前策略风险极高 → 强制切换探索模式
        force_frontier_search()
    elif yao_report.ying_count == 0:
        # 上下无应 → 内部策略与外部环境不匹配
        # 尝试反向操作（如正在取→改为探索）
        switch_to_opposite_intent()
    elif yao_report.cheng_count >= 2:
        # 多处阴乘阳 → 外部阻力强
        # 降低动作速度，增加试探
        set_cautious_mode()
```

### 3.4 关键改动清单（按优先级）

#### P0（必须做，验证易理可恢复）
1. **ContextBuilder**：从world_model + goal → 情景文本
2. **HanziEngine情景感知**：将情景文本送进engine → 情景卦象
3. **差距卦象提取**：scene_yao - task_yao + YaoRelations分析
4. **混合评分**：40%差距卦象 + 40%V18线性 + 20%爻位关系

#### P1（混合互补，提升稳定性）
5. **V18全部veto移植**（不改）
6. **V18 world_model移植**（不改）
7. **V18 stuck恢复逻辑移植** + 新增易理诊断分支

#### P2（降维打击的硬编码剥离）
8. goal_parser：用HanziEngine + YaoRelations取代正则词表（实验性）
9. 6维编码公式：改为"差距卦象映射到候选动作的语义对齐度"而非if-else打分

### 3.5 预期效果评估

| | V17 | V18 | V19（预期） |
|---|---|---|---|
| 易理参与度 | ~100%（全程） | ~4.4%（仅评分） | **~60-70%（大脑决策）** |
| valid_unseen | ~40% | 79.9%（冻结）→98.5%（转导） | **维持~80%（冻结）/ ~98%（转导）** |
| 泛化性（新环境） | 低 | 中低 | **中高（语义理解泛化）** |
| 可解释性 | 高（卦象全程） | 中（veto可解释，卦象不可解释） | **高（卦象+爻位关系+差距诊断）** |

**为什么V19应该能维持V18的性能**：
- 保留V18的工程底座（world_model + veto），这是V18成功的基础
- 新增的易理层只在**存活候选人中做排序**，veto是红线不变
- 混合评分中的40%仍然使用V18的手工6维编码作为"目标基线"
- 差距卦象+爻位关系只在"V18也区分不开的平局"时发挥作用（V18覆盖率92.6%，所以这部分约为7.4%×40%≈3%的影响空间——但差距卦象提供的是语义层面的倾向性，不是纯随机）

**V19真正的新能力**：
- 不依赖词表/正则理解任务（通过HanziEngine语义理解）
- 不依赖统计先验知道物体位置（通过差距卦象诊断"该去哪里"）
- 在veto+stuck恢复之上增加"卦象层面的困境诊断"

## 四、从易理六爻到V18六维：一个统一的语义框架

核心的理论问题：**V18的手工6维编码和真正的易理六爻能不能统一？**

我的答案是：**不需要统一，二者互补**。

```
V18 6维 = 动作对目标的"底线性贡献"
    └── 回答了"这个动作推进目标多少？"（效率视角）
    └── 适合在确定场景下做精确排序
    
易理六爻 = 动作与情境的"整体和谐度"
    └── 回答了"这个动作在当前全局面中是否合道？"（全局视角）
    └── 适合在不确定/边缘场景下做方向性判断
    
差距卦象 = 当前执行偏差的"症状诊断"
    └── 回答了"当前策略在哪个维度出了问题？"（诊断视角）
    └── 适合stuck/失败时的策略修正
```

换句话说：**V18六维告诉agent"怎么更高效"，易理六爻告诉agent"这样做对不对"，差距卦象告诉agent"问题出在哪"**。

## 五、建议的下一阶段实验

1. **Phase 1（V19原型）**：在V18代码基础上，增加ContextBuilder + HanziEngine情景感知，**不做盲板替换**，只在V18的ylyw_scorer中增加"混合评分"模式。验证：混合评分后，冻结首跑成功率是否维持≥79.9%。

2. **Phase 2（硬编码替代）**：逐步用HanziEngine + YaoRelations替代goal_parser和priors.py中的硬编码。验证：成功率维持的同时，词表/先验的依赖是否下降。

3. **Phase 3（跨场景验证）**：用同一个易理推理层，在ALFWorld以外的场景（如机器人操作、导航）验证。验证：卦象空间的语义能否跨环境泛化。

4. **Phase 4（爻位关系对L3活化）**：在差距卦象的6维中嵌入乘承比应当位得中信息，使64卦匹配能产出有意义的策略修正（V18消融中L3贡献≈0，根本原因是输入6维余弦匹配无意义；如果6维带有关系结构，64卦匹配能得出真正的策略建议）。
