#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""在 §5.3 插入架构图，替换 ASCII 流程图"""
t = open('YLYW汉语言文字处理新范式.md', encoding='utf-8').read()

old = '''以下展示文言文单句语义解析的原型系统数据流：

```
输入文言文句子
    ↓
L1 字形层：每个字的偏旁部首识别 → 模糊语义隶属度
    ↓
L2 字义层：根据上下文，从模糊隶属度中确定具体义项
    ↓
L3 词句层：解析字与字之间的"乘承比应"关系，确定句法结构
    ↓
L4 篇章层：（如果是多句）分析句子之间的起承转合关系
    ↓
输出：完整的语义解析 + 可追溯的推理链
```'''

new = '''以下展示文言文单句语义解析的原型系统数据流（如图 1 所示）：

![fig_arch_53](fig_arch_53.png)

**图 1**  文言文单句语义解析原型系统架构：输入经文 → L1字形层（偏旁部首→模糊隶属度）→ L2字义层（乘承比应，159会意字知识库）→ L3词句层（虚词驱动分词、三通道判定）→ L4篇章层（多句起承转合），辅以知几跨句校准与自适应成长底座，输出带可追溯推理链的语义解析。'''

assert old in t, "§5.3 ASCII图未找到"
t = t.replace(old, new, 1)
open('YLYW汉语言文字处理新范式.md','w',encoding='utf-8').write(t)
print("§5.3 已插入架构图（图 1）")
