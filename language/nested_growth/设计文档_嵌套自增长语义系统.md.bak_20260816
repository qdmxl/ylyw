# 嵌套自增长语义系统设计

> 基于 YLYW 分层嵌套结构（字→词→句递归推理），实现**可自适应、可自我繁殖增长**的复杂系统：
> 喂入语料越多 → 繁殖出新语义"元胞" → 每个元胞的 **H 规则自学习** → 系统变复杂、理解力变强。

---

## 一、核心思想：从"固定层级"到"嵌套自增长"

现有多层 YLYW（`hanzi_engine.py`）是**固定三层**：部首→字→词→句，每层一个静态推理单元。

本设计的增量：**系统是可生长的**。它不是固定一套规则跑到底，而是像细胞分裂一样：

```
喂入语料
  ├─ 认识的字/词 → 走已有元胞（用各自H评分）
  ├─ 陌生的字/词/搭配/歧义 → 繁殖出一个"新的语义元胞"
  │     · 新元胞 = 一个独立的 mini-YLYW 推理单元
  │     · 带自己的初始 H 规则权重（5维可学习）
  │     · 挂进嵌套树（作为父系统的子系统）
  └─ 每一局成败 → 参与元胞按灵敏度各校各自的 H
```

**复杂性增长** = 元胞数量增长 + 嵌套层级加深 + 每个元胞 H 变准。
**理解力增长** = 陌生语义识别率、歧义消解、搭配复用持续上升。

---

## 二、元胞数据结构

```python
SemanticCell:                   # 一个最小语义八卦系统
    word: str                   # 该元胞锚定的词/搭配/义项
    parents: [cell]             # 嵌套父元胞（被哪个更大系统包含）
    children: [cell]            # 嵌套子元胞（繁殖出的更细分辨）
    theta: [5]                  # 可学习H权重 [J_adj,J_ying,h_dang,h_zhong,J_comp]
    U_eff: matrix               # 由theta缓存的酉算符
    lh_buf: []                  # 本元胞灵敏度缓冲（局内爻调）
    stats: {encounters, wins, losses, last_use}
    born_step, born_from:       # 繁殖轨迹（出自哪次陌生观察）
    state: active|dormant|...   # 知几激活/知耻休眠
```

**关键**：每个元胞 = 一个完整 mini-QYUF。**不必都真的建 64×64 矩阵**——为性能，元胞内用"六爻向量 + 少量规则"的轻量版本，H 只影响其爻向量的聚合权重（等价于小型H），但保留完整的可学习动力学与灵敏度归因。

---

## 三、繁殖（Growth）规则——"见几而作，不俟终日"

**触发条件**（任一即繁殖）：
1. **陌生词/字**：不在任何元胞词汇表中 → 建新元胞（初始 theta = 父元胞theta 微扰 + 常识先验）
2. **歧义拆分**：一个词同时匹配多个父义项，置信度低于阈值 → 分裂出新元胞专门分辨两者
3. **组合涌现**：多个陌生字频繁以固定搭配出现 → 繁殖"搭配级"元胞（更高层嵌套）

**繁殖时**：
- 新元胞 `theta_new = theta_father ⊕ noise(小)`（先验只增强不覆盖，继承父辈规则）
- 记录 `born_from` 溯源
- 挂接 `father.children.append(new)`
- 立即参与后续推理（知几：见几即用，不俟终日）

**上限**：设 `max_cells`（防失控），达到上限后走"合并/复用"而非无限繁殖。

---

## 四、H 自学习规则——知几/知耻/爻调

每元胞独立学自己的 theta（灵敏度归因，无需反传）：

```
每步决策（该元胞主导时）:
   灵敏度 s_k = ∂P[主导卦] / ∂θ_k    （解析导数，H对θ线性）
   record_decision_learn(cand, yao)  → 存 lh_buf

每局结束（win/loss）:
   θ += α_q · sign · Σ灵敏度        （局内爻调，α_q快=0.1~0.3）
   θ += α_s · 跨局稳定项            （跨局知几，α_s慢=0.01~0.03）
   θ = clip(θ, lower, upper)        （先验钳制，防噪声冲毁）
   rebuild U_eff
```

**不对称性（对齐YLYW"先验只增强不覆盖"）**：成功局强化幅 > 失败局惩罚幅，避免失败把好配置持续拉走。

---

## 五、淘汰与结构演化——知耻

- **activation 衰减**：长期未命中（encounters 少且久）→ state=dormant（休眠保留，不删除先验）
- **高价值强化**：频繁命中且正确 → 允许繁殖更细子元胞（粒度细化）
- 整体**复杂度指标**：`system_complexity = Σcell份量 = 活跃元胞数 + Σ嵌套深度`

---

## 六、评估指标（增长曲线）

| 指标 | 含义 |
|------|------|
| 元胞总数 / 活跃元胞数 | 系统复杂性（应随语料单调增长） |
| 嵌套深度均值/最大 | 结构层次深化 |
| 陌生词识别率 | 理解泛化能力（应上升） |
| 语义匹配准确率 | 理解准确性（应上升） |
| Σ各元胞 θ 变化量 | H 自学习程度（应持续非零） |

---

## 七、最小可跑演示流程

1. 初始：少量种子元胞（如 20 个字/词，从 radical_fuzzy/ideograph KB 播种）
2. 逐句喂入《论语》等语料
3. 每句：分词 → 每个词走对应元胞（无则繁殖）→ 句级综合 → 打语义标签
4. 用"句末是否正确识别语义"作成败反馈 → 各元胞校 H
5. 每隔 N 句记录复杂度/准确率 → 出增长曲线

---

## 八、与既有 h_learnable 的关系

- `qyuf_learnable_h.py`（V22）证明了**单个H可自学习**（θ 演化规律清晰，但单一H话语权受限）
- 本系统**复用同一套灵敏度归因/知几校准动力学**，但**分散到每个元胞**，且增加**结构繁殖**——正是"嵌套多系统"对"单一H"缺陷的补全。

---

## 附录：路线B 最终实现与实验（2026-08-15）

### 关键认知修正（来自马老师指导）
> YLYW 语义理解引擎的本职是把**汉字结构（偏旁部首）拆解并利用其丰富语义信息**。
> 汉字字形(部首)编码的是**语义范畴**（水/火/木/土/金/心/行…→八卦隶属），**不编码伦理学褒贬**。
> 因此"褒贬二分类"在字形层面学不出来（积极/消极词六爻重叠）；**"语义范畴归类"才是汉字真正的语义理解**。

### 任务重新定位：语义范畴归类（8大范畴=八卦）
- 每个字：部首 → 8维八卦隶属度（YLYW L0 基座）→ 主导卦即范畴标签
  - 氵→坎(水)、火→离、木→震、土→坤、金→乾、日→离(明)、心→离 …
- 先天准确率 **68.2%**：radical_fuzzy 含 49 个歧义/脏映射字（六爻平坦、部首冲突），先天 argmax 会判错

### 嵌套增长 + 可学习H 机制
- **字元胞**：学习常见字范畴（先天）
- **词级繁殖**：观察到字组合 → 繁殖词级元胞（嵌套在字系统之上，父=组成字元胞）
- **词元胞 six爻** = 组成字八卦合成；**bias(K_calibration)** = 继承字 bias + 知几校准
- **预测** = `argmax(六爻 + bias)`；bias=0 时纯先天
- **知几校准**（bias 更新）：
  - 判对 → 轻微巩固（`+0.05·lr`）
  - 判错 → 抬正确、压错误（`±0.5·lr`），幅度大（知耻修正）
  - 仅 growth_on 学习，static bias=0

### 实验结果（seed 0，72训练/48测试）
| 方案 | 测试准确率 | 元胞总数 | 词级元胞 | 嵌套深度 |
|------|-----------|---------|---------|---------|
| 静态H | 62.5% | 125 | 0 | 1 |
| 自增长H | **87.5%** | **253** | **128** | max=2, mean=1.51 |

**提升 +25 个百分点**；H(θ) 学习动力学 total_theta_var=7.25

### 多seed稳健性（seed 0-4）
- 静态 mean **65.0%**（52-81波动，方大）
- 增长 mean **89.2%**（85-92稳定，方小）
- **平均提升 +24.2pt**；平均繁殖 116 个词级元胞

### 泛化验证（学术严谨）
- 测试集中 **81.8% 的 2 字词是训练未见过的新组合**（如"通循""盯悟""河浊"）
- 增长系统通过**词元胞继承字 bias** 对这些未见词泛化判对
- 这正是"喂得越多→系统越复杂→泛化理解越强"的核心证据

### 文件
- `exp_growth.py`：实验（语义范畴归类、嵌套增长、bias校准）
- `plot_growth.py` + `growth_result.png`：三张结果图
- `nested_growth_semantics.py`：核心库（SemanticCell/NestedGrowthSemantics）

---

## 附录二：L2 会意字义"统一机制可学性"探测（2026-08-15）

### 马老师核心问题
"嵌套自增长的目的是语义理解。不能为了结构清晰不顾目标。若 L2 也用统一机制，能否实现语义理解？"

### 探测设计与结论（诚实）
对 209 个会意字（`ideograph_knowledge_base.json`），标签=知识库 `bagua_map` 主卦（先验义理卦），
六爻=部首八卦隶属度，验证统一机制能否学出"六爻→字义卦"。

结果：
| 方案 | 测试准确率(含未见字) |
|------|------|
| 先天 argmax | 37-42% |
| 逐字 bias 校准 | +0 (逐字记忆，无法泛化到未见字) |
| 共享 W(8×8) 学习 | -7pt (反而更差) |

### 根源（决定性证据）
按"部首六爻主导卦"分组后，组内"字义卦"分布：
- 部首卦=离 的 44 字，字义卦覆盖**全部 8 种**
- 部首卦=乾 的 26 字，字义卦也覆盖**全部 8 种**
- **"部首六爻 → 字义卦"几乎无统计关联（纯随机）**

### 诚实的学术结论
1. **会意字的"字义义理卦"（仁→乾至善、休→艮止息）是文化先验标注，不是字形涌现**。
   从六爻学它**从原理上不可行**（无统计规律，非算法问题）。
2. **"语义范畴归类"（L0，水/火/木/土/金/心…）才是字形真实承载、可学的语义理解**
   （先天 62-65% → 知几校准 89%，真实+可泛化）。
3. **统一嵌套自增长机制能实现的语义理解 = "字形结构中真实可涌现的语义（范畴）"**；
   **学不出来 = "文化先验约定（会意字义理卦）"** ——后者应定位为 YLYW 的"先验内建"（知识库），
   而非"学习涌现"。这符合 YLYW "先验内建+校准演化" 的路线定位。

### 后续方向建议
- 论文将 L0 语义范畴归类作为"自适应语义理解（学习涌现）"的实证；
- L2 会意字义理定位为"先验内建"，论证统一机制的边界（能学字范畴、不学术文化约定）；
- 或探索句子级语义（词嵌入/搭配统计）作为"可学的语义理解"的更高层。

---

## 附录三：分布语义自组织——阅读→内建知识库（马老师愿景的实证，2026-08-15）

### 马老师核心设想
"真正的语义理解需通过大量上下文的含义理解。先验知识库可用(如人类字典)。关键问题：
**能否通过大量阅读，自己整理/内建知识库？**"

### 方法（distributional semantics / PMI 共现，成熟且与YLYW兼容）
1. 读入大量中文语料（真实论文文本 8.7万字 / 或主题均衡语料 900句×6主题）
2. 统计每个实义字的"同句共现上下文向量"（PMI加权，排除虚词）
3. 余弦相似度 + 平均连接聚类 → **自动浮现语义类**（零人工标注）

### 结果①：从真实语料自动分化语义类（多主题阅读）
系统从 900 句多领域阅读中自动聚类出清晰语义群（无需任何标签）：
- 群【计算技术】算法/模型/神经网络/机器/学习/数据/特征/训练/参数/分类…
- 群【自然物理】水/江河/海/山石/火/燃烧/金属/钢铁/冰霜/森林…
- 群【伦理治理】仁/义/礼/智/信/忠/孝/德/贤/善/恶/君子/小人…
- 群【生命身心】心/情/思/爱/怒/痛/身体/手足/耳目/行走…
- 群【制度规模】国家/君臣/社稷/天地/日月/春秋/年岁…
- 群【学习成长】学习/温故知新/师/朋友/远方/知闻…

### 结果②：阅读量↑ → 语义关联强度↑（成长维度，关键）
| 阅读量 | 学-习 | 计-算 | 水-江 | 仁-德 | 心-思 |
|--------|-------|-------|-------|-------|-------|
| 100句 | 0.83 | - | - | - | - |
| 300句 | 0.93 | 0.92 | - | 0.91 | 0.86 |
| 600句 | 0.95 | 0.95 | 0.91 | 0.92 | 0.88 |
| 900句 | 0.95 | 0.95 | 0.95 | 0.92 | 0.92 |

**读得越多 → 语义关联越强 → 系统"理解"越巩固**（量化单调上升）。

### 结果③：语料涌现 vs 字形部首八卦（互补）
- 对照样例："理"字形部首卦=平坦(无部首信息)，但语料共现给正确语义(模/算/计)
- "学"字形卦=离，语料共现近邻=习/识/知 → 两者一致且互补
- **先天字形(L0部首八卦) + 后天语料共现 = 双通道语义信号**，契合"先验内建+校准演化"

### 结论
**"阅读→自组织内建知识库"成立且可量化**：
- 系统无需人工标注，从大量上下文阅读中自动分化出语义类（这就是自组织的知识库）
- 语义理解强度随阅读量单调增强（读得多懂得多巩固）
- 与字形八卦互补，共同构成"先天(字形)+后天(语料)"的完整语义底座
- 这条能取代/增强人标 `ideograph_knowledge_base`，真正实现"自我繁殖增长的语义理解系统"

### 文件
- build_multitopic_corpus.py：主题均衡语料构造
- exp_self_organize_kb.py：共现聚类→语义群+对照
- exp_distributional_selforganize.py：最近邻语义
- corpus.json / corpus_multitopic2.json：语料

---

## 附录四：方案甲——双通道统一嵌套自增长语义系统（2026-08-15，马老师拍板"甲"）

### 定位
把"先天字形（部首八卦）+ 后天语料（PMI共现）"融合成一个**可成长的统一语义系统**。
见 dual_channel_growth.py（统一引擎）、demo_dual_channel_growth.py（端到端演示）。

### 统一机制
- **字形通道(先天)**：部首八卦六爻 char_sem_vec —— 自然范畴先验
- **语料通道(后天)**：PMI 共现指纹 —— 从大量阅读自组织语义类（替代人标知识库）
- **嵌套自增长+知几**：元胞随阅读繁殖、词嵌套承字bias、双通道判别
- **语义类标签**：语料分布语义自组织产生，无需人工标注

### 关键数据（真实学术语料 8.7万字，7525句）
**语料通道落类准确率 94%**（134/142），各语义类高准确：
计算34/34、伦理20/20、身心20/20、自然26/28、制度19/20、学习15/20。
字形自然范畴与语料类一致（水/江→坎、火/热→离）。

### 端到端成长：G1+G2（读得多→越复杂）
| 阅读句 | 认识字符 | 字元胞 | 词元胞(嵌套) | 总元胞 |
|--------|---------|-------|------------|--------|
| 376 | 580 | 580 | 1,709 | 2,289 |
| 752 | 787 | 787 | 3,240 | 4,027 |
| 1,505 | 966 | 966 | 5,891 | 6,857 |
| 3,010 | 1,169 | 1,169 | 9,686 | 10,855 |
| 5,267 | 1,339 | 1,339 | 14,848 | 16,187 |
| 7,525 | 1,464 | 1,464 | **19,483** | **20,947** |

### 端到端成长：G3 语义收敛（读得多→理解巩固）
| 阅读比例 | 语义类字数 | 稳定率 |
|----------|-----------|--------|
| 5% | 159 | — |
| 12% | 162 | 22% |
| 25% | 162 | 28% |
| 50% | 162 | 33% |
| 75% | 162 | 40% |
| 100% | 163 | **56%** |

**语义划分随阅读量逐渐收敛（22%→56%）** = 系统对语义世界的认识逐渐定形/巩固。

### 三维一致成长结论（方案甲成立）
G1 覆盖面↑（580→1464字符）· G2 复杂度↑（词元胞→19483）· G3 语义收敛↑（22%→56%）
**"读得越多→认识越多→系统越复杂→语义理解越巩固"端到端成立，零人工标注知识库。**

### 文件
- dual_channel_growth.py（统一引擎，SemanticCell双通道/run_growth/run_convergence）
- demo_dual_channel_growth.py + growth_metrics.json
- plot_growth_dual.py + growth_dual.png（三维成长可视化）

---

## 附录五：方案A 真·统一双通道评分（结果 + 诚实边界，2026-08-15）

### 统一评分公式（方案A落地）
score[c] = λ·G(字形六爻→语义类c) + (1-λ)·ctxsim(语料指纹→语义类c) + bias[c]
class = argmax_c score[c]
- G：可学习共享映射 (8维六爻→C维语义类)，先天结构先验
- ctxsim：元胞语料指纹与类质心余弦，后天语义证据
- bias：知几校准

### 结果①（多主题语料6类，含ground-truth）
| 方法 | 准确率 |
|------|--------|
| 纯语料(仅ctxsim) | 93.0% |
| **统一(G+语料+bias)** | **97.7%** |

**融合有真实增益：+4.7pt**。字形结构先验+语料语义证据+知几校准合成单分数，优于纯语料。

### 结果②：真冷启动（语料外字，只有字形）
| 方法 | 准确率 |
|------|--------|
| 纯语料 | 0%（无指纹，彻底失效） |
| 统一字形G | 7%（1/15） |
| 嵌套继承(词=字组合) | 7% |

### 诚实边界（重要，必须写入）
1. 字形六爻→抽象语义类(计算/伦理/制度) **先天是弱信号**（人为约定，无统计规律），
   G 无法仅靠字形泛化到语料外抽象类 —— 与 L2 会意字诊断一致。
2. 统一机制的真实价值 = **在"语料内/语料有证据"时增益**（97.7%>93%）。
3. **不能要求字形通道独立断言抽象语义类** —— 冷启动应由"语料指纹近邻传播"解决：
   新字一旦出现在语料（哪怕1次）就有指纹；字形只作"范畴毛估"的粗糙先验。
4. 稀疏共现实验（freq 1~30）噪声大、不稳定：纯稀疏语料6%~25%波动，
   统一G稳定18.5%，说明字形G提供"稳定的粗糙范畴先验"，但不足以精分类。

### 结论
方案A 的**统一评分机制成立且有增益**，但**字形通道的能力边界是"范畴毛估"而非"抽象类判定"**。
正确的统一架构 = 字形G(粗糙范畴先验) + 语料(精确语义证据) + 知几bias(校准)，三者互补；
冷启动泛化应主要依赖"语料指纹随阅读建立"（嵌套增长），而非纯字形。

### 文件
- exp_unified_score.py（统一评分，97.7% vs 93%）
- exp_unified_coldstart.py（语料外冷启动，诚实展示边界）
- exp_unified_sparse.py（稀疏共现，噪声提示字形G的稳定先验角色）

---

## 附录六：论文章节素材（2026-08-15，马老师拍板"1"—整理进论文）

### 目标
把方案甲成果整理成《YLYW汉语言文字处理新范式》的可插入章节，回应论文§6.7/§7.4的遗留问题
（"系统完全基于人工先验知识库""模糊隶属度需语料校准"）。

### 已产出的论文章节
- `/home/lijinhan/MXL/科研/ylyw/paper/新增章节_语料自组织成长.md`

章节结构：6.8.1动机(先天框架+母语生长) / 6.8.2分布语义基础 / 6.8.3统一双通道评分公式 /
6.8.4语料规模实验(G1覆盖/G2复杂/G3收敛/G4准确率) / 6.8.5自组织语义类示例 /
6.8.6诚实边界(字形不独立推断抽象类) / 6.8.7与§7局限的衔接。

### G4 统一评分多seed确认（5次随机划分）
- 纯语料通道 mean 95.8%
- 统一评分(G+语料+bias) mean 97.7%，稳定，平均增益~+2pt（单次最高+4.7pt）

### 论文核心叙事（与摘要论点衔接）
"从人工抄录的静态字典 → 随阅读持续生长、日益巩固的自组织知识系统"，落实"汉语是母语"论点：
汉字提供先天"象"框架(易理字形)，真正词义理解在大量语境阅读中生长(分布语义)。
