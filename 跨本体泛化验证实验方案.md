# YLYW 跨本体泛化——验证实验方案

> 基于 ICRA 2026 跨本体泛化三条技术路线分析，设计 YLYW 在物理仿真环境中的验证实验
> 版本: v1.0 | 2026-06-28

---

## 一、实验目标

### 核心假设

> **H0**: YLYW 的 64 卦符号系统可以作为跨本体的统一动作表示协议，在零训练数据条件下实现跨本体策略迁移。

### 具体验证点

| 编号 | 验证点 | 预期 |
|---|---|---|
| V1 | 不同本体（单臂/双臂/移动机械臂）能否用同一套卦象规则控制 | 是 (仅需六爻编码重新映射) |
| V2 | 跨本体迁移需要多少数据/工作量 | 零训练数据，仅需卦象校准配置 |
| V3 | 与主流神经方法（LDA-1B/DreamZero/RT-2）相比，成功率差距多大 | 少量差距但零训练代价 |
| V4 | 64卦的离散粒度是否足以完成常见的操作任务 | 粗粒度操作成功率≥80% |
| V5 | YLYW的推理速度（vs 大模型方案）优势 | 至少100x更快 |

---

## 二、仿真平台选择

### 主平台：PyBullet

**理由**：
- 已有安装 ✅
- 内置多种机器人 URDF（kuka iiwa, franka panda, r2d2, husky, humanoid）
- 轻量级，适合快速迭代
- 纯 Python 控制，与 YLYW Python 代码直接对接

### 备选：MuJoCo

**理由**：
- 更高物理精度，适合精细操作验证
- 但需要自行准备多本体模型

### 选型策略：

| 阶段 | 平台 | 本体 | 任务 |
|---|---|---|---|
| Phase 1: 基础验证 | PyBullet | Franka + Kuka + Husky | 抓取/放置/推拉 |
| Phase 2: 精细操作 | MuJoCo | Franka + Shadow Hand | 插拔/开瓶/装配 |
| Phase 3: 移动操作 | PyBullet/MuJoCo | Husky+Arm / Humanoid | 导航+操作 |

> **第一阶段建议从 PyBullet 开始**，因为模型多、上手快。

---

## 三、选择的本体集合

我们从 PyBullet 内置和公开模型中选择 **4 种结构差异足够大** 的本体：

| 编号 | 本体 | 类型 | 自由度 | 末端执行器 | 形态差异 |
|---|---|---|---|---|---|
| R1 | Franka Panda | 单臂机械臂 | 7 | 平行夹爪 | 基准 |
| R2 | Kuka iiwa | 单臂机械臂 | 7+1 | 二指夹爪 | 不同链长/质量分布 |
| R3 | Husky + 机械臂 | 移动操作车 | 4轮+ | 夹爪 | 非固定基座 |
| R4 | 简易双臂 | 双臂操作台 | 7+7 | 双夹爪 | 双臂协调需求 |

### 差异量化

| 差异维度 | R1↔R2 | R1↔R3 | R1↔R4 |
|---|---|---|---|
| 自由度 | 低(同是7轴) | 中(4轮+臂) | 高(7+7) |
| 基座固定性 | 同(固定) | 异(移动) | 同(固定) |
| 末端类型 | 同(夹爪) | 同(夹爪) | 同(双夹爪) |
| 控制接口 | 不同(力矩vs位姿) | 不同 | 不同 |

---

## 四、实验任务设计

从简到难设计 6 个任务，覆盖 ICRA 文章提到的操作类型：

### Task 1: 桌面抓取 (Pick-and-Place)
- **目标**: 抓取桌面物体并放置到目标位置
- **难度**: ⭐
- **适用本体**: R1, R2, R3, R4
- **评价指标**: 成功抓取率、放置精度

### Task 2: 推拉操作 (Push/Pull)
- **目标**: 将物体推至目标区域
- **难度**: ⭐⭐
- **适用本体**: R1, R2, R3, R4
- **评价指标**: 推到位成功率

### Task 3: 容器操作 (Open/Close)
- **目标**: 打开抽屉/门，放入物体再关闭
- **难度**: ⭐⭐⭐
- **适用本体**: R1, R2, R4
- **评价指标**: 操作成功率、步数

### Task 4: 接触密集型 (Contact-Rich)
- **目标**: 擦拭桌面 / 扫除碎屑
- **难度**: ⭐⭐⭐⭐
- **适用本体**: R1, R2, R3
- **评价指标**: 接触力成功率、覆盖率

### Task 5: 长时程 (Long-Horizon)
- **目标**: 多步操作（排序、堆叠多个物体）
- **难度**: ⭐⭐⭐⭐
- **适用本体**: R1, R2, R4
- **评价指标**: 任务完成率、步数效率

### Task 6: 双臂协调 (Bimanual)
- **目标**: 双臂配合开盖、传递物体
- **难度**: ⭐⭐⭐⭐⭐
- **适用本体**: R4
- **评价指标**: 协调成功率

---

## 五、YLYW 跨本体推理架构设计

### 核心思路

```
本体A → 状态感知 → 六爻编码(本体A版) → 64卦规则(通用) → 动作(逆映射为本体A)
本体B → 状态感知 → 六爻编码(本体B版) → 64卦规则(通用) → 动作(逆映射为本体B)
                              ↑
                          /* 跨本体共享! */
```

### 5.1 统一任务描述层

当给定一个任务（如 "pick up the cube"），YLYW 将其解析为八卦基元：

```python
# 统一的任务→八卦映射（跨本体不变）
任务解析表 = {
    "抓取": {"上卦": "艮(止)", "下卦": "巽(入)", "策略": "稳定接近+柔性抓取"},
    "放置": {"上卦": "坤(顺)", "下卦": "兑(悦)", "策略": "轻柔释放+确认到位"},
    "推拉": {"上卦": "震(动)", "下卦": "乾(健)", "策略": "匀速施力+方向修正"},
    "打开": {"上卦": "离(附)", "下卦": "坎(险)", "策略": "先探测+后施力"},
    "关闭": {"上卦": "艮(止)", "下卦": "坤(顺)", "策略": "轻柔归位+确认"},
    "擦拭": {"上卦": "巽(入)", "下卦": "兑(悦)", "策略": "周期性运动+自适应力"},
}
```

### 5.2 本体特异的六爻编码

每个本体需要定义一个 **"本体配置文件"** ——将通用感知信息映射到六爻：

```python
# ---- 本体配置文件示例 ----

# Franka Panda (R1)
本体_R1_六爻映射 = {
    "初爻(基础)": lambda s: s["距离目标"] < 0.1,  # 是否接近
    "二爻(位置)": lambda s: s["物体可抓取"],      # 是否可抓
    "三爻(力)":   lambda s: s["接触力"] > 0.5,    # 是否接触
    "四爻(置信)": lambda s: s["成功率"] > 0.7,    # 置信度
    "五爻(阶段)": lambda s: s["任务进度"] > 0.5,  # 进度
    "上爻(约束)": lambda s: s["环境安全"],         # 安全
}

# Husky (R3) — 多了移动能力，六爻含义不同
本体_R3_六爻映射 = {
    "初爻(基础)": lambda s: s["已到达导航点"],
    "二爻(位置)": lambda s: s["物体在机械臂工作范围内"],
    "三爻(力)":   lambda s: s["轮子打滑"] < 0.2,
    "四爻(置信)": lambda s: s["定位精度"] > 0.05,
    "五爻(阶段)": lambda s: s["导航完成"],
    "上爻(约束)": lambda s: s["电量充足"],
}
```

### 5.3 跨本体共享的64卦规则

关键设计：**L3 的卦象-策略映射表是跨本体不变的**：

```python
# 64卦通用规则表 — 所有本体共享
# 每条规则: {卦名, 六爻模式, 通用策略, 参数模板}
通用卦象规则 = {
    "䷀ 乾为天": {
        "模式": "111111",
        "策略类型": "全力执行",
        "参数模板": {"速度": 1.0, "力": 1.0, "精度": "低"},
    },
    "䷁ 坤为地": {
        "模式": "000000",
        "策略类型": "待机/等待",
        "参数模板": {"速度": 0, "力": 0, "精度": "无"},
    },
    "䷾ 既济": {
        "模式": "101010",
        "策略类型": "完成确认",
        "参数模板": {"速度": 0.3, "力": 0.2, "精度": "高"},
    },
    "䷿ 未济": {
        "模式": "010101",
        "策略类型": "继续尝试",
        "参数模板": {"速度": 0.5, "力": 0.5, "精度": "中"},
    },
    # ... 更多64卦规则
}
```

### 5.4 动作解码器（本体特异）

通用策略 → 本体的具体动作命令：

```python
# 通用参数 → Franka (R1) 关节命令
def 解码_R1(策略参数):
    return {
        "joint_positions": IK_solver(策略参数["末端位姿"]),
        "gripper": 策略参数["夹爪开合"],
        "force_limit": 策略参数["力限制"],
    }

# 通用参数 → Husky+Arm (R3) 命令
def 解码_R3(策略参数):
    return {
        "base_velocity": 策略参数["移动速度"],
        "arm_joint_positions": IK_solver(策略参数["末端位姿"]),
        "gripper": 策略参数["夹爪开合"],
    }
```

---

## 六、对比基线

### 基线方法

| 编号 | 方法 | 类型 | 所需训练数据 |
|---|---|---|---|
| B1 | **YLYW (ours)** | 先验符号推理 | **零** |
| B2 | LDA-1B (隐空间对齐，简化版) | 神经隐空间 | 少量 |
| B3 | Behavior Cloning (动作空间标准化) | 行为克隆 | 大量 |
| B4 | 随机策略 (下限参考) | 随机 | 零 |

### 对比维度

| 维度 | B1 YLYW | B2 LDA-1B-like | B3 BC | B4 Random |
|---|---|---|---|---|
| 训练数据 | 0 | 50-500 episodes | 1000+ episodes | 0 |
| 跨本体迁移成本 | 配置文件 | 微调+数据 | 全量重训 | — |
| 推理速度 | <1ms | ~50ms (GPU) | ~10ms | <1ms |
| 可解释性 | ✅ | ❌ | ❌ | — |
| 成功率(预期) | 中高 | 高 | 中 | 低 |

---

## 七、实验流程

### Phase 1: 单本体基准 (2周)

```
Step 1: 在 Franka (R1) 上实现 YLYW 全套推理链
         - 八卦隶属度计算
         - 六爻编码 (R1版)
         - 64卦规则匹配
         - 动作解码 (R1版)
Step 2: 运行6个任务各20次，记录成功率
Step 3: 对比 Baseline 方法在同一任务上的表现
```

**交付物**: R1 基准性能数据

### Phase 2: 跨本体迁移 (1周)

```
Step 1: 为 R2(Kuka), R3(Husky), R4(双臂) 编写本体配置文件
         (仅需修改: 六爻映射函数 + 动作解码函数)
Step 2: 零样本运行6个任务，记录成功率
         (不修改64卦规则表！)
Step 3: 对比 R1 的结果，分析性能差距
```

**关键测试**: 如果 Phase 2 的性能与 Phase 1 差距 ≤ 15%，则证明跨本体泛化有效。

### Phase 3: 对比实验 (1周)

```
Step 1: 实现系统对比

    实验组:   YLYW (R1→R2, R1→R3, R1→R4)
    对照组1:  YLYW (直接 R2, R3, R4 从头运行)
    对照组2:  BC (在 R1 训练, 迁移到 R2/R3/R4)
    对照组3:  随机策略

Step 2: 各运行20次，统计结果
Step 3: 消融研究
    消融A: 去掉L3规则，仅用L1+L2 + 简单策略
    消融B: 改变六爻编码维度 (6维 vs 4维 vs 8维)
    消融C: 离散化粒度 (64卦 vs 32卦 vs 16卦)
```

### Phase 4: 极限测试 (1周)

```
Step 1: 跨更大差异形态
         - 从 Franka (夹爪) → Humanoid (灵巧手)
         - 从 Franka (固定) → R2D2 (轮式)
Step 2: 零样本 vs 1样本 vs 5样本适配
Step 3: 干扰测试 (增加噪声、延迟、传感器故障)
```

---

## 八、评价指标体系

### 8.1 任务性能指标

| 指标 | 定义 | 统计方式 |
|---|---|---|
| SR (Success Rate) | 任务完成比例 | 20次实验 |
| Steps (步数效率) | 完成任务所需步数 | 均值和方差 |
| Time (时间) | 实际仿真时间 | 均值 |
| Robustness (鲁棒性) | 初始位置扰动下的成功率 | ±10cm位置扰动 |

### 8.2 跨本体泛化指标

| 指标 | 定义 |
|---|---|
| **TGT (Transfer Gap Test)** | `SR_{本体A} - SR_{本体B}` |
| **ZSR (Zero-Shot Rate)** | 零样本在新本体上的成功率 |
| **Adaptation Cost (适配成本)** | 新本体的配置代码行数/时间 |

### 8.3 效率指标

| 指标 | YLYW | 神经方法 |
|---|---|---|
| 推理延迟 | from `time.time()` | from `time.time()` |
| 内存占用 | `memory_profiler` | `nvidia-smi` |
| 训练时间 | 0 | 计时 |

---

## 九、预期结果与论文叙事框架

### 预期结果

```
成功率对比表 (预期):

任务          | YLYW(R1→R1) | YLYW(R1→R2) | YLYW(R1→R3) | BC(R1→R2) | Random
-------------|-------------|-------------|-------------|-----------|-------
桌面抓取     | 85%         | 80%         | 75%         | 70%       | 15%
推拉操作     | 80%         | 75%         | 70%         | 60%       | 10%
容器操作     | 70%         | 60%         | N/A(移动)   | 45%       | 5%
接触密集型   | 65%         | 60%         | 55%         | 40%       | 5%
长时程任务   | 60%         | 50%         | N/A         | 30%       | 2%
双臂协调     | 75%         | N/A         | N/A         | N/A       | 5%
```

### 论文论点结构

```
核心论点: "64卦符号空间——跨本体动作表示的结构化先验"

1. 问题: 跨本体泛化的核心是找到"形态无关的动作表示"
2. 现状: 神经方法需要大量预训练数据
3. 方案: YLYW 用 64卦作为先验符号表示
     - 动作空间标准化: 64卦 = 统一协议
     - 隐空间对齐: 64卦 = 可解释隐空间
     - 世界模型: 64卦序列 = 符号世界预测
4. 实验: 4个本体 × 6个任务 × 3个基线
5. 结果: 零样本跨本体泛化，与弱基线竞争，与强基线有差距但差距可接受
6. 意义: 符号先验是跨本体泛化的一个重要补充方向
```

### 与 ICRA 文章的定位关联

```
"以动作空间标准化构建统一的数据协议"           ← YLYW 擅长的
"以视频模型统一路线提供丰富的视觉世界先验"       ← 可以API调用来补充
"再以隐空间对齐实现精细的动力学建模"              ← L3 + 本体微调可以实现
```

---

## 十、实施计划

### 10.1 代码结构

```
ylyw/cross_embodiment/
├── README.md
├── configs/
│   ├── body_R1_franka.py      # 本体R1配置文件
│   ├── body_R2_kuka.py        # 本体R2配置文件
│   ├── body_R3_husky.py       # 本体R3配置文件
│   └── body_R4_bimanual.py    # 本体R4配置文件
├── core/
│   ├── trigram_base.py        # L1: 八卦基元 (跨本体共享)
│   ├── yao_encoder.py         # L2: 六爻编码 (调用本体配置)
│   └── hexagram_rules.py      # L3: 64卦规则 (跨本体共享)
├── adapters/
│   ├── franka_adapter.py      # R1动作解码器
│   ├── kuka_adapter.py        # R2动作解码器
│   ├── husky_adapter.py       # R3动作解码器
│   └── bimanual_adapter.py    # R4动作解码器
├── tasks/
│   ├── task_pick_place.py
│   ├── task_push_pull.py
│   ├── task_open_close.py
│   ├── task_wiping.py
│   ├── task_long_horizon.py
│   └── task_bimanual.py
├── baselines/
│   ├── baseline_random.py
│   ├── baseline_bc.py         # 行为克隆 (简单MLP)
│   └── baseline_lda.py        # 简化隐空间对齐
├── experiments/
│   ├── run_phase1.py          # 单本体基准
│   ├── run_phase2.py          # 跨本体迁移
│   ├── run_phase3.py          # 对比实验
│   └── run_phase4.py          # 极限测试
├── analysis/
│   ├── plot_results.py        # 图表生成
│   └── statistics.py          # 统计分析
└── results/                   # 实验结果输出
```

### 10.2 时间线

| 阶段 | 内容 | 预估时长 |
|---|---|---|
| Phase 0 | 搭建 PyBullet 仿真环境 (4个本体) | 3天 |
| Phase 1 | 在 Franka 上跑通完整推理链 | 5天 |
| Phase 2 | 跨本体迁移 (R2,R3,R4 零样本) | 3天 |
| Phase 3 | 基线对比实验 + 数据统计 | 5天 |
| Phase 4 | 极限测试 + 消融实验 | 3天 |
| 论文撰写 | 整理结果、制图、撰写 | 5天 |
| **总计** | | **~24天** |

### 10.3 风险点与缓解

| 风险 | 概率 | 影响 | 缓解措施 |
|---|---|---|---|
| PyBullet 仿真不够精细 | 低 | 中 | Phase 2 切部分实验到 MuJoCo |
| 64卦粒度不够细 | 中 | 高 | 引入"爻值连续量"作为精细参数 |
| 跨本体成功率下降过多 | 中 | 高 | 分析下降原因，设计"本体自适应补偿层" |
| 神经基线(B2,B3)性能过高 | 低 | 低 | 精确定位"训练数据需求"的对比 |
| 双臂仿真实现复杂 | 高 | 中 | Phase1-3 可跳过 R4，作为 Bonus |

---

## 十一、下一步行动建议

### 🥇 第一优先

**搭建 PyBullet 环境 + Franka 基础任务场景 (Phase 0)**

具体：
1. 加载 Franka Panda URDF
2. 放置桌面 + 物体（立方体、圆柱、球）
3. 实现基础的 6 自由度末端控制
4. 将 YLYW L1-L3 推理输出与末端控制对接

### 🥇 第二优先

**编写 YLYW 跨本体核心模块**

1. 提取跨本体共享的 `trigram_base.py`, `hexagram_rules.py`（直接从已有 `api_docs/` 中提取）
2. 设计本体配置文件的标准化格式
3. 实现 L2 六爻编码的动态加载

### ⚡ 执行顺序建议

```
Day 1-3:  PyBullet + Franka 环境跑通 (找马老师一起确认)
Day 4-7:  编写 Franka 的 YLYW 完整推理链
Day 8-10: 添加 Kuka 和 Husky 本体
Day 11-13: 零样本跨本体测试
Day 14-18: 基线对比 + 消融
Day 19-21: 数据分析 + 制图
Day 22-24: 论文撰写
```
